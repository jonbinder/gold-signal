# Dormant SignalScore UI

These components (`SignalScoreHero`, `ScoresInContrast`, `ScoreReferenceGuide`, `SignalScoreSupplement`) are not imported by any route. The public `/signalscore` page is now the About page.

Revive by wiring components back into `src/app/(main)/signalscore/page.tsx` and restoring scoring on user-facing surfaces.
