# Dormant SignalScore UI

These components (`SignalScoreHero`, `ScoresInContrast`, `ScoreReferenceGuide`, `SignalScoreSupplement`) are not imported by any route. The public `/signalscore` URL is the How It Works / About page (nav label: "How It Works").

Revive by wiring components back into `src/app/(main)/signalscore/page.tsx` and restoring scoring on user-facing surfaces.
